name: 🚀 Deploy to Kubernetes (Helm)

on:
  workflow_run:
    workflows: ["🏗️ Build & Push Docker Image"]
    types: [completed]

jobs:
  deploy:
    runs-on: [self-hosted]   # EC2 Runner (이미 쿠버네티스 클러스터 접근 가능)

    env:
      NAMESPACE: hiteen
      CHART_PATH: ./hiteen-chart
      IMAGE: ghcr.io/${{ github.repository_owner }}/hiteen-api
      TAG: ${{ github.event.workflow_run.head_sha }}

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🔧 Set up kubectl
        uses: azure/setup-kubectl@v4
        with:
          version: v1.30.0

      - name: 🧰 Set up Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.14.0

      - name: 🔐 Configure kubeconfig
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG }}" | base64 --decode > ~/.kube/config
          chmod 600 ~/.kube/config
          kubectl config set-cluster kubernetes --insecure-skip-tls-verify=true
          echo "✅ Current context:"
          kubectl config current-context
          kubectl get nodes -o wide


      - name: 🧱 Create namespace if not exists
        run: |
          kubectl get ns $NAMESPACE || kubectl create ns $NAMESPACE

      - name: 🪶 Deploy via Helm
        run: |
          helm upgrade --install hiteen $CHART_PATH \
            -n $NAMESPACE \
            --set app.image.repository=$IMAGE \
            --set app.image.tag=$TAG \
            --set ingress.enabled=true \
            --wait --timeout 300s

      - name: 🔍 Verify rollout
        run: |
          kubectl rollout status deployment/hiteen-test -n $NAMESPACE
          kubectl get pods -n $NAMESPACE -o wide

      - name: ✅ Success notification
        if: success()
        run: echo "✅ hiteen deployed successfully!"

      - name: ❌ Failure notification
        if: failure()
        run: echo "❌ Deployment failed. Check logs."
